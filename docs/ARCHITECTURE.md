# Architecture

Technical companion to the [README](../README.md). Everything here describes the code as it
stands. If something drifts, the code wins, and an issue is welcome.

---

## Design principles

Four decisions shape the whole codebase. They're worth stating up front, because most of the
structure follows from them.

**1. The model writes prose, code writes decisions.**
Scores come from the model; the verdict does not. `server/pipeline/verdict.js` computes
`APPLY NOW` / `IMPROVE FIRST` / `LOW PRIORITY` / `DO NOT APPLY` from the artifacts, in plain
JavaScript, and is covered by edge-case tests (`test/unit/verdict.test.mjs`, run with
`npm run test:unit`). Likewise the Markdown report (`report.js`) and
the PDF exports (`exports/`) are assembled from JSON, never generated by a model. The same
analysis always yields the same verdict and the same document.

**2. Nothing is asserted without evidence.**
`cv_extractor` builds an Evidence Bank from the CV; every later claim must map back to it;
`quality_controller` blocks the ones that don't and can force a single regeneration. The
shared contract lives in `server/prompts/_core_rules.md` and is prepended to every agent
prompt.

**3. The engine is a detail.**
Pipeline messages are `{ type: "text" | "file", path, label }`. Each provider materialises
files its own way: the API provider sends base64 image/document blocks, the CLI provider
passes paths and lets the agent's own Read tool open them, the bridge hands the agent a list
of local paths. Adding an engine is adding one file.

**4. State is files on disk.**
No database, no daemon, no session store. An application is a directory; a bridge job is a
directory. You can read, diff, back up and delete everything with a file manager, which is
also what makes the privacy guarantee checkable rather than promised.

---

## Repository map

```
server/
  index.js                 Express API, static frontend, bridge endpoints
  config.js                .env loading, provider resolution (hot-swappable)
  bridge/
    queue.js               On-disk job queue + bridge state (heartbeat, history)
    tasks.js               Free-form requests sent from the Console page
  llm/
    provider.js            Engine selection
    claude_session.js      Default: the agent bridge
    claude_cli.js          Headless `claude -p` sub-process
    anthropic.js           Anthropic API
    mock.js                Canned data (MOCK_DELAY_MS to watch the UI progress)
    prompt_builder.js      Shared prompt + JSON Schema construction, Zod repair pass
  research/
    research_provider.js   Company web search (via the active engine, or none)
  extract/
    files.js               PDF → text (pdf-parse), image/document blocks
  exports/
    documents.js           Summary + improvement plan, assembled in code
    pdf.js                 Markdown → PDF (pdfkit, no headless browser)
    artifacts.js           Artifact loading helpers
  prompts/
    _core_rules.md         The integrity contract shared by every agent
    <agent>.md             One prompt per agent, the substance of the project
  pipeline/
    orchestrator.js        The 16 stages, parallelism, quality retry
    schemas.js             Zod schemas for every structured output
    verdict.js             Deterministic final verdict
    report.js              Markdown report assembled from artifacts
  storage/
    applications.js        Application folders, versions, metadata

bridge/
  cli.mjs                  Agent side: wait / status / show / answer / fail
  queue/<jobId>/           One job = job.json + prompt.md + schema.json → result.json

web/                       Vanilla ES modules, no build step
  index.html               Shell + sidebar
  styles.css
  js/{main,dashboard,application,bridge,ui,api}.js

demo/seed.mjs              Creates one fictional application and analyses it
test/unit/                 Unit tests for the deterministic parts (verdict thresholds)
test/e2e.mjs               Full workflow against a running server
test/bridge.mjs            Bridge only, no server, no session

applications/<id>/         YOUR DATA, git-ignored
  input/job/               The posting (screenshots, PDF or text)
  input/v1..vN/            CV + letter, per version
  research/                Company research memo and notes
  analysis/shared/         Artifacts shared across versions
  analysis/v1..vN/         Per-version artifacts
  output/v1..vN/           report.md, summary and plan (Markdown + PDF)
  logs/vN.log              Human-readable log of each analysis
```

---

## The sixteen stages

| # | Stage | Agent | Produces |
|---|---|---|---|
| 1 | Posting | `job_parser` | Normalised posting + requirement matrix (`MUST_HAVE` / `STRONG_SIGNAL` / `NICE_TO_HAVE` / `CONTEXT` / `CULTURAL_BEHAVIORAL`, explicit vs inferred, estimated weight) |
| 2 | Company research | `company_research_search` → `company_researcher` | Findings tagged `FACT` / `STRONG_INFERENCE` / `WEAK_INFERENCE`, each with a source |
| 3 | Recruiting thesis | `recruiting_modeler` | Mission thesis, top 5 business problems, realistic ideal candidate |
| 4 | Opportunity | `opportunity_scorer` | Score /100, is this posting worth your effort at all |
| 5 | Evidence Bank | `cv_extractor` | Everything the CV actually proves, as addressable items (`E1`, `E2`, …) |
| 6 | Mapping | `requirement_mapper` | Requirement → evidence, strength, and `POSITIONING_GAP` vs `ACTUAL_EXPERIENCE_GAP` |
| 7 | Evaluation | `cv_evaluator` | Score /100 across four filters: ATS, HR screen, hiring manager, strategic fit |
| 8 | Adversarial review | `adversarial_reviewer` | Reasons to reject you in twenty seconds, with severity and probability |
| 9 | Benchmark | `competitive_benchmark` | Your position against the likely applicant pool |
| 10 | Letter analysis | `letter_evaluator` | Letter score and diagnosis (skipped when no letter is provided) |
| 11 | Improvement plan | `improvement_planner` | Changes ranked by return on effort |
| 12 | CV rewrite | `cv_optimizer` | Rewritten CV + before/after per bullet |
| 13 | Letter rewrite | `letter_optimizer` | Rewritten letter + key changes |
| 14 | Quality control | `quality_controller` | `PASS` / `FAIL` + unsupported claims. A `FAIL` triggers one regeneration |
| 15 | Comparison | `version_comparator` | Improved / regressed / still blocking, and the stop condition (v ≥ 2 only) |
| 16 | Report | (code) | Deterministic verdict + Markdown report + exports |

Stages 8, 9 and 10 run in parallel, as do 12 and 13. Stages 1-4 are **shared**: computed once per
application and reused by every later version, so a v2 only re-runs what depends on the CV.

Stop conditions on re-evaluation: `APPLICATION_READY` (score ≥ 88, or marginal gains ≤ 2 points)
or `APPLICATION_STRUCTURALLY_WEAK` (the remaining gaps are real experience gaps that rewriting
cannot close).

---

## The agent bridge

The default engine (`claude-session`) is the agent session you're already talking to. The
protocol is deliberately boring:

1. The server writes `bridge/queue/<jobId>/` containing `job.json` (title, application, files to
   read, whether web search is expected), `prompt.md` (the complete prompt) and `schema.json`
   (the JSON Schema derived from that stage's Zod schema).
2. The agent claims the job (`claim()` is atomic, so several listeners can't collide), reads the
   prompt and the referenced files itself, does the work, and writes `result.json`.
3. The server validates against the Zod schema. On failure it republishes a repair job containing
   the exact validation error; **one** repair attempt is allowed.
4. Completed jobs move to `bridge/archive/`. The heartbeat in `bridge/agent.json` is what drives
   the "bridge connected" indicator in the UI.

If no agent is listening, the pipeline **waits** (up to `CLAUDE_SESSION_TIMEOUT_MS`, 45 min per
stage by default). It does not fail, and it resumes as soon as a listener comes back.

The CLI wrapper:

```bash
node bridge/cli.mjs wait --session "my-session"   # listen, exits when a job appears
node bridge/cli.mjs status                        # queue + heartbeat
node bridge/cli.mjs show <jobId>                  # inspect a job
node bridge/cli.mjs answer <jobId> result.json    # structured answer
node bridge/cli.mjs answer <jobId> --text-file a.md   # free-form (Console requests)
node bridge/cli.mjs fail <jobId> "reason"         # give up on a job
```

Run **one listener at a time**. Starting a second without stopping the first just stacks
processes that all eventually exit; `claim()` keeps it harmless, but it's noise.

---

## HTTP API

Everything the frontend does, you can do with `curl`.

| Method | Route | Purpose |
|---|---|---|
| `GET` | `/api/health` | Active engine, bridge state, capabilities |
| `GET` | `/api/applications` | Dashboard list |
| `POST` | `/api/applications` | Create (multipart: `screenshots[]`, `cv`, `letter`, `name`) |
| `GET` | `/api/applications/:id?version=N` | Metadata + all artifacts for a version |
| `POST` | `/api/applications/:id/analyze` | Start the pipeline (`{ version }`) |
| `GET` | `/api/applications/:id/progress?version=N` | Live stage status + log tail |
| `POST` | `/api/applications/:id/cancel` | Stop a running analysis |
| `POST` | `/api/applications/:id/versions` | Upload a v(n+1) (multipart: `cv`, `letter`) |
| `GET` | `/api/applications/:id/report?version=N` | Full Markdown report |
| `POST` | `/api/applications/:id/submit` | Mark as sent (`{ submitted, version }`), reversible |
| `GET` | `/api/applications/:id/export/:kind?version=N&format=md\|pdf` | `kind` = `synthesis` or `plan` |
| `GET` / `POST` | `/api/bridge`, `/api/tasks` | Bridge state and free-form Console requests |
| `POST` | `/api/settings` | Hot-swap the engine |

```bash
curl "http://localhost:3777/api/applications/<id>/export/synthesis?version=1&format=pdf" -o summary.pdf
```

The summary (verdict, scores, four-filter diagnosis, rejection risks, integrity check) and the
improvement plan are also written automatically at the end of every analysis, into
`applications/<id>/output/v<n>/`. A rendering failure is logged and never fails the pipeline.

---

## Extending it

**Add an engine.** Create `server/llm/<name>.js` exporting
`{ name, complete({ stage, system, messages, schema, webSearch, log }) }` and returning
`{ data, text }`. Register it in `server/llm/provider.js` and in `PROVIDERS` in `config.js`.
Your job is to decide how `{type: "file"}` messages become something your model can see.

**Add a search provider.** `server/research/research_provider.js` is the seam. Anything that
returns sourced text works: an MCP server, SerpAPI, a local index.

**Retune the prompts.** `server/prompts/` is plain Markdown, one file per agent. Two places
currently assume a junior/VIE profile and are worth adjusting for other markets: the
`vie_details` field in `job_parser`, and `recruiting_modeler`'s assumption of a 0-2 years ideal
candidate. Both already ask the agent to flag the assumption when the posting contradicts it.

**Change the schemas.** `server/pipeline/schemas.js` must import from **`zod/v4`**: the Anthropic
SDK's `zodOutputFormat` helper and `z.toJSONSchema` both break with the classic v3 API. This has
been fixed once; please don't regress it.

---

## Known constraints

- **pdfkit standard fonts are WinAnsi.** Em dashes, `œ` and `€` go through an allow-list in
  `sanitize()`; anything outside it is silently dropped. Extend the list rather than the font
  unless you want to ship a font file.
- **Windows command-line length.** The `claude-cli` provider passes prompts through stdin and
  spawns with `shell: true` (the npm shim is a `.cmd`).
- **`claude-cli` and expired OAuth.** A 401 "OAuth token expired" means you need to run `claude`
  and `/login` again; the error is mapped explicitly so you don't have to guess.
- **The bridge dies with the session.** That's expected: the UI shows it, and `npm run bridge`
  brings it back. Analyses in flight wait rather than fail.
